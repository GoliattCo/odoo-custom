<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!--      purchase.order: Inherit Form (Payment Tracking)         -->
    <!-- ============================================================ -->
    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.co_warehouse_extended</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='purchase_delivery_invoice']" position="inside">
                <group string="Supplier Payment Tracking">
                    <field name="co_purchase_request_id" readonly="1"/>
                    <field name="co_fulfillment_pct" widget="progressbar"/>
                    <field name="co_fulfillment_state" widget="badge"
                           decoration-warning="co_fulfillment_state == 'partial'"
                           decoration-success="co_fulfillment_state == 'complete'"/>
                    <field name="co_payment_state_summary" readonly="1"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
